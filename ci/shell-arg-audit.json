{
  "schema_version": "astrolabe.shell_arg_audit.v1",
  "sites": [
    {
      "file": "crates/astrolabe-bridge/src/lib.rs",
      "function": "watch",
      "call": "cbm_watcher_watch",
      "validator": "validate_shell_arg(root_path)",
      "payload_contract": "watch root path is validated before passing to CBM watcher git-status shell paths",
      "coverage": "astrolabe-bridge::tests::watcher_rejects_inputs_cbm_would_silently_ignore exercises injection-shaped root paths and verifies zero watch rows are accepted"
    }
  ]
}
