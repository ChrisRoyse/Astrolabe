# ASTROLABE — Agent Operating Manual

ASTROLABE fuses Calyx (Rust association-native DB engine, at `calyx/`) with codebase-memory-mcp (C code-graph MCP server, at `cbm/`) into one Rust-host binary (`crates/astrolabe-server`) with the C half linked as `libcbm.a` (`crates/cbm-sys`, `crates/astrolabe-bridge`). **Both parent codebases are ASTROLABE's own first-class source (owner directive, 2026-07-12): pulled in once, owned outright, edited directly, evolved in place on a stable base — this is its own project built from the original code of both repos plus the custom code that connects them. "Vendoring" is dead: no pins, no patch overlays, no upstream-refresh procedure, no upstream-tracking of any kind. The origin repos are at most a read-only quarry to copy useful code from as normal development. EPIC #286 dismantled the residual vendoring machinery (pins/patch overlays/`VENDORED.md`) and relocated the trees out of `vendor/` to top-level `calyx/` and `cbm/` (Phase B).** Plan of record: `docs/astrolabe-blueprint.md`. Binding upstream doctrine: `docs/BUILDING_ON_CALYX.md` (the Calyx builder's handbook the blueprint derives from — embed-vs-encode, atoms → all base associations → differentiate → kernel → compose, the anti-pattern list; on conflict the blueprint's code-domain decisions win, recorded there as design corrections). **State of record: GitHub issues — nothing else.**

## The one rule that outranks all others

**GitHub issues are the single source of truth for what is done, in progress, and remaining.** The blueprint describes the *design*; it never records *progress*. If you learn something about project state, it goes in an issue comment. If it isn't on an issue, it didn't happen.

## Execution context (mandatory)

- **Canonical workspace:** all repository work starts from and remains rooted at `C:\code\Astrolabe`. Run commands with that exact directory as the working directory. Do not use a copied checkout, shadow tree, or project-owned output directory outside this workspace.
- **Native Windows build toolchain:** produce build/test evidence with the native Windows GNU toolchain via the launcher, because the Rust host and the static `libcbm.a` archive must share one ABI (see Build notes). Use PowerShell and native Windows executables; run POSIX scripts under a Git for Windows Bash executable (e.g. `C:\Program Files\Git\bin\bash.exe`) for a consistent, pinned toolchain rather than an arbitrary shell.
- **WSL coexistence (do not fight the host):** WSL is a normal, permitted part of this Windows machine. Project scripts, the launcher, and gates MUST NOT treat WSL — its services, packages, distributions, `wsl.exe`, `wslservice`, `vmmemWSL`, or `C:\Windows\System32\bash.exe` — as a fault condition, must never fail closed on its presence, and must never stop, disable, uninstall, or DISM-service it. Do not build anything that wipes, repairs, or polices WSL to keep the host "pure"; a system that deletes the operator's WSL install is a defect, not a safety property. The only reason evidence runs natively is ABI/target match (below), not hostility toward WSL.
- **Native-target evidence:** Astrolabe ships as a native Windows binary, so build/test/gate evidence must be produced by the native Windows toolchain from the canonical workspace — a build for a different target (including a Linux or WSL build) exercises different code paths and does not satisfy a Windows DoD. This is about matching the shipping target, not excluding WSL. A gate that genuinely cannot run natively must be reported and tracked.
- **Windows-only until fully operational (owner directive, 2026-07-11):** ASTROLABE targets **Windows and no other system** until the whole system is operational and built. Do not do cross-platform work now — no Linux/macOS builds, no Linux-host runbooks, no cross-platform goldens, no sanitizer-on-Linux plans, no portability refactors "while we're in here." Porting is a deliberate **final phase**, tracked in the `Port — cross-platform (deferred)` milestone; it is not started, not scheduled early, and not a blocker for any Windows DoD. A DoD clause whose evidence owner is a non-Windows platform is **not** a coverage gap to apologize for and **not** a runbook someone should execute now: it is `DEFERRED[ASTRO_PORT_PHASE]` — labeled, counted, tracked on #238, and **never presented as passing** (invariants 1 and 3 still bind; deferral is disclosed, not silent). An issue may close on native-Windows evidence alone once its non-Windows clauses are re-pointed at the port milestone. Deferral is not abandonment: we intend to port, at the end, once Windows works.
- **Autonomous project decisions:** make and execute the technical, environment/toolchain, prioritization, and implementation choices required to reach ASTROLABE's intended end state. Choose the most robust, durable, project-aligned option rather than a locally easier workaround; do not pause for routine approval or leave a known next step undecided.
- **FSV-only verification (owner directive, 2026-07-13; supersedes the 2026-07-12 test-doctrine and tiered-gate directives):** the aggregate gate suite, check scripts, gate configs, suite-impact machinery, and gate self-tests are **retired and deleted** — test suites of that kind are judged a waste of wall-clock and are not to be rebuilt. **Full State Verification against reality is the only verification doctrine:** build the real artifact, exercise the real behavior (real data, real stores, never mocks), then **independently read back the persisted state** (bytes on disk, DB rows, process output, registry/ledger entries) and compare it to the claim; probe the relevant edge cases the same way. Evidence = the command, the execution context, and the read-back state, recorded on the driving issue. `cargo build`/`cargo check` proving the artifact compiles, plus a direct FSV of the changed behavior, is the closure bar.
- **Manual FSV only — ALL tests deleted (owner directive, 2026-07-14; supersedes the 2026-07-13 directive's test-tolerance):** passing tests prove **nothing** — "not a single thing can be determined as working if it passes a test; only manual full state verification of reality is this check." Every test was deleted from the tree: all `#[cfg(test)]` modules, all `tests/` directories (crates, `calyx/`, the cbm C suite), trybuild suites, and test scripts. **Never write, run, or cite a test again — for verification, evidence, measurement, or closure.** Verification is done by hand: build the real artifact natively, exercise it yourself with real calls against a real codebase (this repo's own trees are an always-available corpus), independently read back the persisted state (bytes on disk, DB rows, process output, ledger entries) and compare it to the claim; probe edge cases the same way. Measurements (latency, recall, convergence) are taken against the real running binary/server — never via a test harness. `cargo check`/`cargo build` remain valid as artifact-buildability evidence; they are builds, not tests.
- **Batch verification (owner directive, 2026-07-15):** consolidate, never repeat — one native build serves all pending close-gates of a session/wave; orchestrator readback probes for multiple issues run as one combined script in one pass; GitHub mutations (claims, evidence comments, closes) are batched per round; workers/lanes preserve built binaries + fixtures + drivers to the session scratchpad so verification re-runs artifacts instead of rebuilding them. Reuse proven FSV drivers from the scratchpad and prior-issue comments before writing new ones.
- **Continuity and state:** GitHub issues and their comments are the durable state record. On every resume or context compaction, re-read `AGENTS.md`, this file, the active issue and all of its comments, and the live Git/worktree state before acting. This is a sole-agent repository: a prior `status:in-progress` claim never prevents continuation.
- **Reality-first closure:** an implementation, test, gate, or workflow is evidence only after direct verification against its real artifact, process, browser state, or persisted data. Record the command, execution context, and result in the issue before closure. After every issue close, commit and push the verified state immediately.
- **Frozen tree during evidence builds (#424):** the checkout being built — canonical workspace or worktree — must be frozen from the moment an evidence build starts (any build whose artifact will be exercised for FSV) until that artifact's FSV readback is captured. Any edit to that checkout inside the window voids the evidence and requires a rebuild; a build that "happened to win the race" against a mid-build edit is not evidence. FSV evidence records binary-vs-tree provenance: the commit SHA of the built tree and a hash of the produced artifact, recorded together on the driving issue, so every readback is attributable to an exact tree.
- **Enforced Git mutation boundary (#519):** the dedicated launcher owner holds the operating worktree's exact Git `index.lock` with write-through/delete-on-process-exit semantics and retains deny-write/delete handles over every tracked/visible-untracked source plus the bound Git index/HEAD/ref metadata. Index-backed porcelain therefore fails before its first index/worktree write even with hooks overridden; the `reference-transaction` hook is the ref-only backstop. Never remove or bypass a live interlock. Read-only Git and mutation in a different registered worktree remain available.
- **Synapse and hygiene:** browser work uses only the already-open authenticated Chrome session through Synapse, opening new background tabs there rather than launching a browser. Keep worktrees, branches, temporary tools, and outputs clean. When work reveals a problem outside the active fix, create a tracked GitHub issue rather than silently carrying it forward; report unavailable Synapse capabilities in `ChrisRoyse/synapse` when that prevents needed verification or control.
- **Workspace-local outputs:** repository-controlled build products, test databases, logs, fixtures, and temporary outputs must stay under `C:\code\Astrolabe`, normally under `target/`, and must never be staged or committed.
- **Reserved namespace is not fixture space (#750):** a manual FSV fixture must never be created as a direct child of canonical `.tmp` with a launcher-reserved prefix (`windows-gnu-toolchain-`, `.astro-launcher-temp-cleanup.`, `no-escape-attribution-`, or attribution cleanup/stage names). Exercise product recognition with the reserved-looking component beneath an ordinary issue-scoped fixture root, such as `.tmp\manual-fsv\issue-N\edge\.tmp\windows-gnu-toolchain-fixture`; this preserves the input semantics without impersonating launcher protocol state. A foreign top-level reserved TEMP directory blocks admission and is never path-deleted. Only `scripts\archive-foreign-launcher-state.ps1`, with exact owner-authored tracker evidence and retained FILE_ID/inventory readback, may move one conclusively foreign empty ordinary TEMP directory into the append-only archive; attribution state keeps its separate manifest/stage recovery protocol.
- **Clean after every use:** every build, check, or explicit contiguous verification batch owns its `target/` output, and the exact live launcher owner must remove that output immediately after evidence is captured, including on failure or interruption. A batch is supplied only as `-BatchCommandsJson` containing at least two nonempty command string arrays; one dedicated process executes it fail-fast while continuously retaining the same v3 lease, Job, target handle/FILE_ID, and durable ownership manifest. The ambient `ASTROLABE_CONTIGUOUS_BATCH` variable is forbidden and refuses before mutation. The canonical launcher trampoline transfers every admitted root to the hash-verified v3 authority at `C:\code\Astrolabe\scripts\windows-gnu-toolchain-authority.ps1`; the launcher then holds a strict schema-v3 lease at `.tmp/astrolabe-launcher.lock` with the canonical authority/trampoline/helper hashes. Ownership is exact `(pid, owner_process_start_utc_ticks, issue)` with a complete evidence fingerprint, and the live launcher retains a no-write/no-delete-share protocol handle plus a no-delete-share target handle. Only the authoritative classifier's `absent` state permits a new claim. Held, version-mismatch, stale/PID-reused, unreadable, interrupted-transition, and process-query-fault states never authorize ordinary deletion or target cleanup. Final cleanup durably records two equal exact target inventories, handle-renames the same root beneath generation TEMP, deletes only finalized FILE_ID-bound identities, and publishes source/tombstone absence before releasing the protocol. If live/unevaluable attributed children or target state force preservation, treat that cleanup error as a blocker: never delete `target/` by hand, complete tracker-bound lock recovery after the process tree is dead, then clean only under a fresh exact launcher lease. Verify `target/` absent before unrelated work and every pause, stop, issue close, turn end, or handoff.
- **Immutable native FSV artifacts (#596/#612):** a native binary that will be exercised after Cargo output is cleaned must be promoted from launcher-owned `target/` during the same issue-owned launcher process with `scripts\native-fsv-artifact.ps1 -Operation Stage`. Execute and inspect only the resulting content-addressed artifact through `scripts\native-fsv-run.ps1` while that launcher lease remains live; the runner denies artifact/application-directory deletion and persists artifact/output/tree readback. Every launcher, promoter, runner, and child owner is recorded as one strict `(pid, process_start_utc_ticks)` identity. After the launcher exits and authoritatively removes its protocol and `target/`, use `-Operation Cleanup` only when a separate exact-generation read proves all recorded owners inactive; PID reuse is diagnostic state, never impersonation, and an unevaluable query preserves every byte. If orchestration fails before any run state exists, a pristine two-file session may be removed only through `-Operation Abandon`: it refuses locks/live or unevaluable receipt owners/extra files and persists an external hash-bound abandonment record before exact session removal. If the real child created bound live/output state but the runner terminally failed before publishing a valid run record, use only `-Operation Quarantine`: pass the exact live-state and expected run-record paths; it refuses either lock or any live/unevaluable exact owner, hashes every ordinary session file into an external append-only recovery record, and only then removes that exact session. A valid completed run is Cleanup-only and a pristine session is Abandon-only. Legacy PID-only v1 state fails closed and may be removed only through tracker-bound `-Operation MigrateLegacy`, after all legacy numeric PIDs are absent and an owner-authored comment binds the immutable receipt/inventory hashes and external migration record. Missing creation ticks are never inferred. Direct deletion, cross-lifecycle cleanup, and closure FSV from disposable `target/` are invalid.
- **Multi-session lock discipline (#197/#611/#617/#613/#614):** (1) launcher-lock ownership is exact Windows process identity `(pid, owner_process_start_utc_ticks)`, not numeric PID; the current v3 lease also binds issue, lease ticks, command, frozen-tree fingerprint, canonical authority/trampoline/helper hashes, exact attribution manifest, generation-specific TEMP directory, and deterministic non-breakaway `Global\` Job Object. (2) Claim, exact-owner cleanup, and explicit reclaim serialize across sessions on one `Global\` mutex keyed by the opened root's filesystem identity. Before that root-local claim, every admission also crosses one shared CUDA-retirement `Global\` mutex keyed by the retained canonical-workspace identity. Retirement publishes a durable exact `(pid,start ticks,issue)` transition, accepts only an explicitly bound exact caller-self v3 lease, includes every stable NUL-safe Git-registered worktree or refuses it by name, inventories locks/bundles twice under the transition, then handle-renames the exact attested obsolete root to a transaction tombstone and durably reads back intent/completion/post-state evidence. Matching live identity is inviolable; an observed or unevaluable Job is preserving even with zero listed members. Version-mismatch, stale, unreadable, interrupted-transition, and unevaluable state all fail ordinary acquisition closed. (3) Reclaim only after re-reading the issue and posting exact path/hash/owner/probe evidence. `scripts\reclaim-launcher-lock.ps1` must verify that `gh` comment, probe owner and Job twice, durably read back authorization/finalization/completion, and atomically archive unchanged bytes plus its exact bound subordinate inventory. Fresh pre-v2 or owner-unreadable state without deterministic Job derivation is permanently preserving; legacy/quarantine switches only resume an already-published strictly linked marker or quarantine an exact malformed subordinate stage under a valid modern transition. (4) Tracker-comment before acquiring/removing any lock and re-read the issue immediately first. (5) Multi-hour runs use `scripts\detach-run.ps1` with mandatory independent observer-readiness and launcher-lease budgets. Production is explicit `InteractiveToken`; `S4U` is an opt-in isolated-local protocol FSV mode only and never a fallback. A start result is accepted only after durable runner/boundary/work records prove exact work identity equals the authoritative held v3 owner; publish all three exact identities at start. Readiness timeout preserves task/process state without stop/delete. The append-only `.tmp\detached-runs\<run-id>` hash chain — intent, full task definition/security readback, runner, boundary, work, held lease, completion, and cleanup or fault — is the source of truth. Before any `Add-Type`, coordinator and runner create an exact compiler scope beneath that run and durably bind its owner; normal cleanup double-inventories and handle-renames only that scope, while any compiler/security ambiguity preserves it with a structured fault. Anonymous direct `.tmp` compiler children are forbidden. Legacy opaque compiler state is recovered only through `scripts\recover-detach-compiler-state.ps1` Prepare/Probe/tracker/Recover evidence. (6) Lock FSV uses isolated fixture roots only.
- **Launcher publication share continuity (#619):** Win32 enforces READ/WRITE share access **per file** and DELETE share access **per link**. A handle that withholds `FILE_SHARE_WRITE` therefore denies writes through every hard link of that file, including a link that does not exist yet; a handle that withholds `FILE_SHARE_DELETE` protects only the link it was opened through. Every scratch-to-final publication must keep a write-denying handle continuously live from before the published link is created until the immutable published lease is established — the delete-on-close anchor holds no write access so the guard can coexist with it, the transient writer is the only write-capable handle and is closed before publication, and the published lease is opened **before** any scratch handle is released. The published lease deliberately holds no `FILE_WRITE_DATA`: a write-capable lease cannot be opened while the guard is live and would reintroduce the writable window. Because `FlushFileBuffers` requires `GENERIC_WRITE`, immutable leases route through `Invoke-AstroExactLeaseDurabilityFlush`, which **declares** the skip (`ASTRO_LAUNCHER_IMMUTABLE_LEASE_FLUSH_DECLARED`) — data durability is owned by the pre-publication scratch flush and the object can never change again. Never reintroduce an all-sharing "observer" as protection: sharing WRITE/DELETE is an identity bridge only, never an immutability lease. The remaining per-link DELETE gap means an equivalent-token peer can still **unlink** a published link in the link→lease window; that can never mutate the object, fails closed at `validate-immutable-published-lease` with source preserved, and grants no capability a peer lacks anyway (it can squat the claim path before the launcher starts). Distinct principals remain constrained by the workspace/protocol ACL, and project processes serialize through the identity-derived `Global\` mutex.
- **CUDA no-space view lifecycle (#707):** expected junctions are classified at creation as mount-point reparses and bound to retained no-delete-share handles, exact raw reparse bytes, namespace/target paths, target FILE_ID, and a durable generation manifest. Only after the child/Job barrier may the exact live owner delete those retained junction objects and independently prove every installed target identity unchanged; generic TEMP inventory runs afterward against the now-ordinary tree. A missing/changed manifest, changed expected junction, or unexpected foreign reparse fails closed and remains preserving. Installed CUDA target bytes are never deletion candidates.
- **No CI/CD — banned (owner directive, 2026-07-11):** GitHub Actions and every other hosted CI/CD system are banned in this repository. Nothing may be written for, configured in, or justified by `.github/` workflows, required status checks, or any CI job — do not create `.github/` content, do not cite a CI job as coverage owner, do not defer evidence "until CI runs." Verification is local Full State Verification only, executed from the canonical workspace with output recorded on the issue.
- **Security is a non-goal — no security work (owner directive, 2026-07-25; #740):** ASTROLABE runs on one machine, for one operator, against local repositories. There is no multi-tenant surface, no hostile peer, no untrusted caller. **Do not perform, plan, or file security work** — no threat models, hardening passes, injection/supply-chain screening, sandboxing, secret scanning, egress restriction, privacy/compliance erasure, encryption at rest, ACL hardening against hostile principals, or CVE tracking. Never add security clauses ("an attacker cannot…", "malicious input…", "untrusted source…") to an issue Scope or DoD; a security finding from any review is closed `not planned` citing #740. The `area:security` label is retired. This does **not** touch correctness: fail-closed refusals, structured `{code, message, remediation}` errors, labeled degradation, and the standing invariants are integrity properties and remain binding; validating malformed input (bad UTF-8, truncated JSON, oversized rows, path limits) prevents wrong answers and stays; drift/integrity codes (`ASTRO_SOURCE_DRIFT`, `CALYX_LENS_FROZEN_VIOLATION`) exist so the system never lies about what it read. The launcher lock/handle protocol stays too — it stops this operator's concurrent sessions from destroying each other's build evidence — but its threat-model-flavored clauses (hostile equivalent-token peers, foreign-principal impersonation, ACL hardening) are non-goals and must not be extended. Correctness guards such as shell-argument validators remain; if one blocks valid work, relax or remove it rather than harden it.
- **No host servicing by project tooling:** project scripts and the launcher perform no operating-system servicing — no DISM, no Windows-feature enable/disable, no WSL install or uninstall — and must not fail closed on transient OS-servicing state. Host administration is the operator's concern, outside the build/test loop. (The former exclusive-host-maintenance / WSL-removal machinery has been retired; see the closed WSL-direction issues.)

- **Dead-generation recovery (#735):** never retry an ordinary launcher while any owned `target/` or dead complete TEMP/manifest pair remains; ordinary admission refuses a non-absent target before publishing a new protocol generation. After independently proving the exact owner generation and deterministic Job absent and posting the required machine-readable issue comment, archive its lock/transition with `scripts\reclaim-launcher-lock.ps1`, recover only the hash-bound preserved target through `scripts\windows-gnu-toolchain.ps1 -RecoverPreservedTarget ...`, then archive each complete dead pair with `scripts\archive-launcher-state.ps1`. The target recovery and pair archive are distinct durable transactions: the former must finish and independently read back `target/` absent before the latter can run. `reclaim-launcher-lock.ps1` does not archive a complete pair, and ordinary launcher admission grants no recovery authority. Preserve every byte on any identity, hash, Job, tracker, or readback mismatch.

## Issue workflow (mandatory, in order)

1. **Pick**: choose an issue labeled `status:ready` in the lowest-numbered open milestone (phases must complete in dependency-spine order: `P0 → P1 → P2 → (P3 ∥ P4) → P5 → P6 → (P7 ∥ P8) → P9`; see EPIC #65). Never start a `status:blocked` or `status:needs-spec` issue.
2. **Claim**: comment on the issue: what you're about to do, your plan in ≤5 bullets. Swap label `status:ready` → `status:in-progress`. If the issue already has `status:in-progress` and a claim comment newer than 48h, pick a different issue.
3. **Verify the claim**: re-read the issue body **and all comments** — progress comments record what prior sessions already completed. Never redo checked DoD items; verify them instead (re-run the named manual FSV against the real artifact) and note the result.
4. **Work in scope**: implement ONLY what the issue's Scope section says. Discovered adjacent work = file a new issue with labels + milestone + a `Blocked by:`/`Blocks:` line, and link it in a comment. Never expand scope in place.
5. **Commit discipline**: every commit message body must reference the issue (`Refs #N`, or `Closes #N` on the final commit). No commit may touch `docs/astrolabe-blueprint.md` to record progress — status prose in the blueprint is banned (design corrections are fine).
6. **Prove, then check the box**: a DoD checkbox may only be checked in the same session that ran its verification (command + result pasted in a comment). Verification is FSV: persisted-state readback, not API echoes.
7. **FSV before done**: build the real artifact natively, exercise the changed behavior against real data, and independently read back the persisted state that proves it (see **FSV-only verification**). Paste the command, native execution context, and read-back evidence in the closing comment. If a needed verification genuinely cannot run, file/link an issue for the gap — never skip silently. The exact live launcher owner performs repo-local `target/` cleanup in its `finally` path; independently verify absence. A non-absent protocol state or deferred attributed child blocks closure and never authorizes manual deletion.
8. **Close**: when every DoD box is checked with evidence, close the issue, tick its checkbox in EPIC #65, and move the `status:*` label off. If you must stop early, comment exactly: what's done, what's not, the next concrete step, and any local uncommitted state; swap to `status:ready` if another agent can resume, or `status:blocked` naming the blocker.

## Standing invariants (every PR — the HONEST conjunct; violating these = do not merge)

1. No unlabeled claim — grounded responses carry `trust`, `freshness`, `provenance`.
2. No ungated confidence — refusal with deficit over confident guessing.
3. No silent fallback — every degradation labeled, every skip counted.
4. No constant that could be a measurement — new thresholds/weights are registry-declared knobs or measured values.
5. State verification over green checkmarks — manual FSV reads persisted bytes (never a test's green output), determinism is proven by repeated real runs compared byte-for-byte, and every mutation is paired with its ledger entry read back from the store.
6. Production-ready or not merged — no `todo!()`/stubs on shipped paths; fail-closed `{code, message, remediation}` errors; docs on public APIs.

## Layout

- `crates/astrolabe-domain` — identity spine (canonical_input_bytes, CxId/series). `astrolabe-ingest` — SQLite→vault import, registry, projections, ledger verify. `astrolabe-lower` — vault→SQLite lowered artifact, team artifact. `astrolabe-panel` — lens panel S0–S22. `astrolabe-weave` — similarity graphs, cross-terms, reactive, anomalies. `astrolabe-kernel` — kernels, bridges, label propagation, skills. `astrolabe-guard` / `astrolabe-oracle` / `astrolabe-assay` / `astrolabe-anchors` / `astrolabe-provenance` — contract crates being brought live per phase. `astrolabe-bridge` + `cbm-sys` — FFI to libcbm. `astrolabe-server` — MCP surface.
- ⚠️ `crates/astrolabe-server/src/migration.rs` is under decomposition (see the refactor issue). **Do not add new code to it**; new MCP tool logic goes in the module structure that issue defines.
- Verification: manual FSV only (see Execution context). The former `scripts/check-*.sh` gate suite and `ci/` gate configs were deleted 2026-07-13; **all remaining test code (`#[cfg(test)]`, `tests/` dirs, cbm C suite, trybuild, test scripts) was deleted 2026-07-14 (owner directive)** — neither is ever to be rebuilt. There is no CI: GitHub Actions is banned (see Execution context).
- `calyx/` and `cbm/` (top-level, relocated out of `vendor/` in #286 Phase B) are **owned first-class source** (owner directive 2026-07-12; EPIC #286) — edit them directly like any other code in this repo, with the same quality bar. The legacy pin/overlay machinery (`VENDORED.md`, `scripts/verify-pins.sh`) was dismantled under #286; do not reintroduce it, do not consult it as authority. `patches/cbm/` is not overlay machinery — it is Astrolabe-owned libcbm build glue (`Makefile.cbm` + the `ASTRO_*` translation units) compiled directly by `cbm-sys/build.rs`. Sibling checkouts (`C:\code\Calyx`, `C:\code\Calyx-Dev`, …) are never workspaces — at most read-only sources to copy useful code from.

## Build notes

- Toolchain: pinned via `rust-toolchain.toml` (Rust 1.95, edition 2024). The C half needs GNU make + a C toolchain (`cbm-sys/build.rs` respects `MAKE`/`CC`/`CXX`/`AR`).
- Pure-Rust work: `cargo check -p <crate> --all-targets` is fast; full workspace check requires the C toolchain.
- Windows: full native builds use a `x86_64-pc-windows-gnu` Rust host toolchain plus MinGW so Rust and the static libcbm archive share one ABI; the default MSVC host toolchain is not valid for that link. Bootstrap the pinned Rust-CI-compatible GCC 14.1 bundle, LLVM 20.1 analysis bundle, and Cppcheck 2.20.0 source build with `powershell -ExecutionPolicy Bypass -File scripts\windows-gnu-toolchain.ps1 -Issue <driving-issue> -Bootstrap`; run every native Cargo command through that launcher with the positive driving issue so the matching runtime and pinned lint tools are selected and the launcher lock records its tracker owner. It confines child `TEMP`/`TMP`/`TMPDIR` to a launcher-owned `.tmp` child in the workspace and removes both that child and `target/` on normal exact-owner exit; live or unevaluable attributed children cause a fail-closed preservation/error, never a partial cleanup. Every cargo invocation under the launcher MUST pass `--locked` (#1021): the #519 freeze holds a deny-write handle over `Cargo.lock`, so a cargo command that wants to rewrite the lockfile dies with os error 32 before compiling; `--locked` is the correct fail-closed behavior under a frozen tree (a stale lockfile then fails with cargo's own structured error instead of a handle conflict). From native PowerShell, pass one command's arguments as JSON, for example: `$toolArgs = '["build", "--workspace", "--locked"]'; .\scripts\windows-gnu-toolchain.ps1 -Issue <driving-issue> -Command 'cargo' -CommandArgsJson $toolArgs`. For a contiguous batch, omit `-Command`/`-CommandArgsJson` and pass a nested plan such as `$batch = '[["cargo","check","--workspace","--locked"],["cargo","build","--workspace","--locked"]]'; .\scripts\windows-gnu-toolchain.ps1 -Issue <driving-issue> -BatchCommandsJson $batch`; it runs inside one exact owner and stops at the first nonzero child. Record unavailable coverage honestly. Never fake a verification result; file/link any coverage gap.
- Worktree layout and authority (#226/#436/#613): the launcher accepts a build root only from the canonical workspace `C:\code\Astrolabe` or a registered Git worktree parked directly under `C:\code\Astrolabe\.claude\worktrees\<name>` and bound to the canonical Git common directory. Each keeps its own `target/`, `.tmp/`, and session lock while sharing the canonical pinned `.toolchains`/`.sccache`; every entrypoint is only a byte-identical trampoline to the canonical v3 authority. Provision with `scripts\launcher-worktree.ps1 -Operation Provision -Name <name> -Commitish <immutable-commit>`; raw `git worktree add` is not a supported launcher admission path. Provisioning computes the widest exact-owner generation plus every launcher-owned native scratch descendant and refuses names whose ordinary DOS path would exceed the pinned GCC/MinGW 259-visible-character boundary; the authority independently repeats that contract before creating `.tmp` on every launch. Retire a clean, lock/transition/attribution-free historical root with `scripts\launcher-worktree.ps1 -Operation Retire -Name <name>` after the required issue-bound exact probes. The manager probes process references twice, moves the exact same root directory object (verified by NTFS file identity) under `.claude\retired-worktrees`, and preserves any inert ignored evidence/target bytes in place within that object; the retired layout then refuses launcher execution. Version-skewed/misregistered/path-incompatible roots refuse before mutation with structured canonical remediation. `-Bootstrap` stays canonical-only.
- Native formatter: use `python scripts/native-cargo-fmt.py --all -- --check` for the complete workspace and its local path dependencies. Bare `cargo fmt --all` cannot run this graph on Windows because upstream cargo-fmt creates one command line beyond the OS limit; the checked-in runner preserves Cargo's target traversal and invokes rustfmt in bounded batches.
- Target hygiene: local cargo/build/check commands may create or grow `./target`. Treat it as disposable evidence workspace, not persistent state. Have the exact live launcher owner remove `./target` immediately after each command or contiguous verification batch, whether it passes, fails, or is interrupted, and independently verify absence. Never infer deletion authority from `Test-Path`, a stale lock, or a numeric PID. If exact ownership cannot be proven, preserve the state and file/link the blocker; tracker-reclaim non-absent protocol state before any fresh exact-owner cleanup. Also verify absence before issue close, turn end, pause, stop, or handoff. Stop a process only through a retained handle or after independently proving its exact `(pid, process-start ticks)` identity belongs to this session; numeric PID alone never grants stop/cleanup authority.
