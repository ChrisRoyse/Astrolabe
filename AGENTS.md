# Astrolabe Agent Context

`CLAUDE.md` is the repository operating manual and must be read before doing any work. GitHub issues are the state of record.

The execution boundary is non-negotiable:

- Dead-generation recovery (#735): never retry an ordinary launcher while an owned `target/` or dead complete TEMP/manifest pair remains. Ordinary admission refuses a non-absent target before publishing a new protocol generation. After exact owner-generation and deterministic-Job absence plus the required machine-readable tracker comment, archive the lock/transition with `scripts\reclaim-launcher-lock.ps1`, recover only the hash-bound target through `scripts\windows-gnu-toolchain.ps1 -RecoverPreservedTarget ...`, then archive each complete pair with `scripts\archive-launcher-state.ps1`. These are distinct durable transactions; target absence must be read back before pair archival. Preserve every byte on any identity, hash, Job, tracker, or readback mismatch.

- Work only from the canonical checkout at `C:\code\Astrolabe`.
- Pipeline cost is a correctness property (2026-08-08; #1064). Before writing, extending, or wiring any data-processing path — indexing, parsing, embedding, weave, kernel, ingest, MCP request handling, vault/ledger IO, build glue — read #1064 (binding defect-class catalogue, PC-01..PC-43). State the cost function against real production N and name what is invariant across the loop; `unknown N` is a tracked gap, a guess is not. The default open is the narrowest one: name the column families, read-only unless you write, no MVCC row restore unless you need rows. A cost claim in a comment is a hypothesis with an owner and an expiry — cite its measurement (N and date) or delete it (PC-41). Manual FSV fixtures run ~1,000x below the real index, so a passing fixture FSV has measured no cost at all. Determinism is a prerequisite for parallelization, never a follow-up (PC-38). Cite the `PC-nn` class in the issue and the commit.
- Produce build/test evidence with native Windows processes and the native GNU toolchain, because the Rust host and the static `libcbm.a` archive must share one ABI. Run POSIX scripts under a Git for Windows Bash executable (e.g. `C:\Program Files\Git\bin\bash.exe`) for a consistent, pinned toolchain.
- Native reproducibility boundary (#791): the MinGW `libcbm` partial link must retain `--no-insert-timestamp`; the combined COFF localizer rejects a nonzero header timestamp and never routes through `objcopy`; `ar rcsD` is explicit and the archive-header readback must prove zero timestamp/UID/GID plus exactly one size-matched `libcbm.o` before Rust links it. The final Rust/PE link independently retains the launcher-owned zero-timestamp control, and the shipping shim's exact `--version` output exposes the native reproducibility contract used to produce it. Never patch produced bytes or substitute `SOURCE_DATE_EPOCH` as a fallback.
- WSL coexistence: WSL is a normal, permitted part of this Windows machine. Project scripts, the launcher, and gates must never treat WSL (services, packages, distributions, `wsl.exe`, `wslservice`, `vmmemWSL`, `C:\Windows\System32\bash.exe`) as a fault condition, must never fail closed on its presence, and must never stop, disable, uninstall, or DISM-service it. Do not build anything that wipes or polices WSL to keep the host "pure." Evidence runs natively only to match the shipping target, not out of hostility toward WSL.
- Native evidence requirement: a build for a different target (including a Linux or WSL build) exercises different code paths and does not satisfy a Windows DoD; re-run required evidence natively and record the Windows execution context in the issue. A verification that genuinely cannot run natively is a coverage gap to file, not a host to modify.
- Run native Cargo and C build commands through the canonical launcher trampoline `scripts\\windows-gnu-toolchain.ps1`; it transfers every canonical/worktree invocation to the hash-verified v3 authority at `C:\code\Astrolabe\scripts\windows-gnu-toolchain-authority.ps1`, pins the matching GNU runtime and LLVM analysis tools, confines temporary files to a launcher-owned workspace child, and removes that child and `target/` on normal exact-owner exit. A worktree trampoline mismatch refuses before target, TEMP, Git, or shared-tool mutation. If attributed children remain live or their state is unevaluable, the launcher preserves the entire owned state and exits with a cleanup error. It holds a fail-closed schema-v3 lease at `.tmp/astrolabe-launcher.lock`; ownership is the exact Windows process identity `(pid, owner_process_start_utc_ticks)`, never a numeric PID alone. Only the authoritative classifier's `absent` state permits a new claim. `held`, version-mismatch, stale/PID-reused, unreadable, interrupted-transition, and process-query-fault states never authorize ordinary deletion or target cleanup. A contiguous native batch is one `-BatchCommandsJson` nested string-array plan executed fail-fast by one dedicated launcher owner; the same v3 lease, Job, retained target handle/FILE_ID, and durable target manifest remain live from exact target creation through handle-bound tombstone cleanup. `ASTROLABE_CONTIGUOUS_BATCH` is forbidden ambient authority and always refuses.
- Multi-session lock discipline (#197/#611/#617/#613/#614): claim, exact-owner cleanup, and explicit reclaim serialize across Windows sessions through one `Global\` mutex derived from the opened launcher root's filesystem identity. Every launcher admission first crosses the shared CUDA-retirement `Global\` mutex derived from the retained canonical-workspace identity; retirement publishes an exact `(PID,start ticks,issue)` transition, accepts only an explicitly bound exact caller-self lease, inventories every `git worktree list --porcelain -z` root twice, and durably records the handle-bound tombstone/delete/post-state readback. A matching live process identity is inviolable (never remove its state, stop its process, or clean `target/` under it). Every current v3 lease binds the canonical protocol authority/trampoline/helper hashes, an exact attribution manifest, generation-specific TEMP directory, and deterministic non-breakaway `Global\` Job Object; an observed or unevaluable Job is preserving even when its member list is empty. Retired v2, stale v3, or transition-bound partial state is archived only after re-reading the owning issue, posting exact PID + creation-ticks + path/hash probe evidence, and using `scripts\\reclaim-launcher-lock.ps1`, which independently verifies the `gh` comment, probes owner/Job twice, and reads back durable authorization/finalization/completion/archive bytes. Fresh pre-v2 or owner-unreadable state without deterministic Job derivation is permanently preserving; legacy/quarantine switches only resume an already-published strictly linked marker or quarantine an exact malformed subordinate stage under a valid modern transition. Tracker-comment before acquiring or removing any lock and re-read the driving issue first. Launch multi-hour runs through `scripts\\detach-run.ps1` with separate mandatory positive observer-readiness and launcher-lease budgets; production uses explicit `InteractiveToken`, while `S4U` requires the isolated-local-FSV opt-in and is never a fallback. Accept readiness only when its durable exact work identity equals the authoritative held v3 owner, post the runner/boundary/work identities at start, and treat a readiness timeout as preserving — the observer never deletes the task or stops a process. The append-only run directory below `.tmp\\detached-runs\\<run-id>` and its strict hash-linked intent/task/runner/boundary/work/lease/completion/cleanup-or-fault records are the detached source of truth. Coordinator and runner `Add-Type` imports occur only after that run exists, inside exact owner-bound compiler children whose intent/inventory/handle-rename/completion or fault records remain in the run; anonymous direct `.tmp` compiler children are forbidden. A known compiler nonzero is durably recorded and cleaned by that still-live exact owner before the outer failure is published; unknown callback/identity/inventory faults remain preserving. Recover a modern dead-owner run-local scope only through `scripts\\recover-detach-run-compiler-state.ps1` Prepare/tracker/Recover while its launcher mutex, exact owner-absence probes, script/record hashes, FILE_ID, security, and triple-equal inventory remain bound. Recover opaque legacy compiler state only with `scripts\\recover-detach-compiler-state.ps1` through its normal-token Prepare, same-principal elevated Probe, exact owner-authored tracker line, and resumable handle-bound Recover stages. Lock FSV uses isolated fixture roots only.
- Detached task terminal recovery (#1066): a real detached run whose append-only chain reached `005-launcher-lease.json` but never persisted cleanup may retain its exact Task Scheduler entry. Never retry the ordinary launcher or delete that task ad hoc. Use `scripts\\recover-detach-task.ps1` Prepare, post its exact machine-readable line on the source issue, then Recover. The transaction binds the source issue, chain head, task record/XML, every recorded process generation, principal, scripts, launcher/target/Git-lock absence, and two stable Scheduler reads; Recover positively acquires the canonical launcher mutex, verifies the owner comment twice, writes authorization and finalization before `DeleteTask`, proves absence twice through fresh Scheduler services, appends one terminal recovery record without rewriting the run, and completes the external chain. Any process, XML, chain, tracker, mutex, protocol, or readback mismatch preserves the task and every byte. Only the same immutable finalized transaction may resume an interrupted post-delete readback.
- CUDA no-space view lifecycle (#707): expected junctions are bound at creation to retained no-delete-share handles, exact raw reparse state, target FILE_ID, and a durable generation manifest. After the child/Job barrier the exact live owner deletes only those junction namespace objects, proves every installed target identity unchanged, and only then permits generic TEMP inventory. A missing/changed manifest, changed expected junction, or unexpected foreign reparse remains fail-closed and preserving; never traverse or delete installed CUDA targets.
- Frozen Git mutation boundary (#519): while an exact launcher lease is live, the dedicated owner retains the operating worktree's Git `index.lock` as a write-through, delete-on-process-exit interlock and holds deny-write/delete handles over every tracked/visible-untracked source plus the bound Git metadata. Never remove or bypass that interlock. Read-only Git remains allowed, and a different registered worktree remains independent.
- Project scripts and the launcher perform no operating-system servicing — no DISM, no Windows-feature enable/disable, no WSL install or uninstall — and must not fail closed on transient OS-servicing state. Host administration is the operator's concern, outside the build/test loop.
- The launcher builds the pinned Cppcheck 2.20.0 source commit with its matching GNU compiler and retains only the active launcher-managed Cppcheck cache root.
- Both parent code trees are owned first-class source (owner directive 2026-07-12; EPIC #286): edit them directly with the normal quality bar. The legacy overlay + pin machinery was dismantled under #286; do not reintroduce it. `patches/cbm/` is not overlay machinery — it is Astrolabe-owned libcbm build glue compiled directly by `cbm-sys/build.rs`.
- **Security is a non-goal (owner directive 2026-07-25; #740):** this system runs locally, for one operator, on one machine. Never perform, plan, or file security work — no threat models, hardening, injection/supply-chain screening, sandboxing, secret scanning, egress restriction, privacy/compliance erasure, or CVE tracking — and never add security clauses to an issue Scope or DoD. Security findings are closed `not planned` citing #740; `area:security` is retired. Correctness is untouched: fail-closed refusals, labeled degradation, malformed-input validation, drift/integrity codes, and the launcher lock protocol all stay (the launcher protects this operator's concurrent sessions from each other, not against an attacker — do not extend it toward hostile-peer defense).
- **No CI/CD (banned, owner directive 2026-07-11):** GitHub Actions and all hosted CI/CD are banned. Never create `.github/` content, cite a CI job as coverage owner, or defer evidence "until CI runs." All verification is local full-state verification, recorded on the issue.
- Keep repository-controlled outputs inside the workspace, normally in `target/`.
- Manual FSV fixtures must never be direct children of the canonical `.tmp` directory whose names begin with launcher-reserved `windows-gnu-toolchain-`, `.astro-launcher-temp-cleanup.`, `no-escape-attribution-`, or attribution-cleanup/stage prefixes. To exercise descendant-path refusal, create the reserved-looking component below an ordinary issue-scoped fixture root (for example `.tmp\manual-fsv\issue-N\edge\.tmp\windows-gnu-toolchain-fixture`) so it cannot impersonate protocol state. A foreign top-level reserved TEMP directory remains fail-closed and is never manually deleted; archive only a conclusively foreign empty ordinary TEMP directory through `scripts\archive-foreign-launcher-state.ps1` after exact tracker evidence. Attribution state remains under its distinct manifest/stage recovery protocol.
- Make the exact live launcher owner perform `C:\code\Astrolabe\target` cleanup after every build/verification batch, including failure or interruption, and independently verify absence before any pause, stop, issue close, turn end, or handoff. The owner pre-creates the target, retains its no-delete-share handle and FILE_ID, durably finalizes two equal exact inventories, atomically handle-renames that same root beneath its generation TEMP, deletes only the finalized identities, and publishes terminal source/tombstone absence before releasing the protocol. Never delete or rename a target manually on the strength of a numeric PID, ambient variable, or path check. If launcher protocol state is non-absent or attributed-child/target state prevents exact-owner cleanup, preserve every byte, record the blocker, and complete the tracker-bound reclaim/ownership procedure before cleanup.
- Native binaries used after Cargo cleanup must be promoted before cleanup with `scripts\native-fsv-artifact.ps1 -Operation Stage`, then executed and inspected only through `scripts\native-fsv-run.ps1` while the same issue-owned launcher lease remains live. Every persisted launcher, promoter, runner, and child owner is a strict exact identity `(pid, process_start_utc_ticks)`; the content-addressed receipt/tree/artifact hashes and run record are the source of truth. After the launcher exits and authoritatively removes its protocol and `target/`, use `-Operation Cleanup` only after a separate probe proves every recorded exact owner generation inactive; an unrelated live process reusing a numeric PID is recorded as reused, while an unevaluable identity preserves all state. A pristine staged session that never ran is removed only with `-Operation Abandon`, after it persists an external hash-bound abandonment record and proves every receipt owner generation inactive. A terminal partial run whose runner could not publish a valid run record is removed only with `-Operation Quarantine`: it requires the exact bound live-state and expected run-record paths, proves every receipt/live-state owner generation inactive and both locks absent, inventories and hashes every ordinary session file into an external append-only recovery record, then removes only that session. Valid completed runs remain Cleanup-only; pristine sessions remain Abandon-only. PID-only v1 sessions are never consumed normally: retire one only through `-Operation MigrateLegacy`, after all legacy numeric PIDs are absent and a fresh owner-authored issue comment binds the receipt hash, full session inventory hash, external record path, and exact removal action. Never infer missing process-start ticks or execute closure FSV directly from disposable `target/`.
- **FSV-only verification (owner directive, 2026-07-13; supersedes the 2026-07-12 tiered-gate and test-doctrine directives):** the gate/check suite, gate configs, suite-impact machinery, and gate self-tests are retired and deleted — do not rebuild them. Verification is Full State Verification against reality: build the real artifact natively, exercise the real behavior with real data (never mocks), independently read back the persisted state, probe the relevant edge cases, and record command + context + readback on the driving issue. **Manual FSV only — no tests (owner directive, 2026-07-14):** all test code was deleted from the repo (`#[cfg(test)]`, `tests/` dirs, the cbm C suite, trybuild, test scripts); passing tests prove nothing. Never write, run, or cite a test for verification, evidence, measurement, or closure. Verify by exercising the real artifact yourself against a real codebase and reading back persisted state by hand; measurements run against the real binary/server. `cargo check`/`cargo build` remain valid buildability evidence.
- Make and execute the technical, environment/toolchain, prioritization, and implementation decisions needed to reach ASTROLABE's intended end state. Prefer the most robust, durable, project-aligned solution over a locally easier workaround; do not pause for routine choices or leave known next steps undecided.
- GitHub issues and their comments are the durable state record. At every resume or context compaction, re-read this file, `CLAUDE.md`, the active issue and its comments, and the current Git/worktree state before acting. A claimed or `status:in-progress` issue is still available to this sole agent.
- Treat a result as real only after direct state verification of the actual artifact, process, browser state, or persisted data. Record that evidence in the issue before closing it; commit and push the verified repository state after every issue close.
- Use the already-open authenticated Chrome session through Synapse for browser work, opening only background tabs in that browser. Keep worktrees, branches, temporary tools, and outputs clean; create and track a GitHub issue for every discovered problem that is outside the current fix.
- For the complete Rust formatter, run `python scripts/native-cargo-fmt.py --all -- --check`. Do not run bare `cargo fmt --all` on native Windows: upstream cargo-fmt submits the Calyx graph as one Windows-overlimit process.
- `calyx/` and `cbm/` (top-level, relocated out of `vendor/` in EPIC #286 Phase B) are ASTROLABE's own code, not upstream sources. No pins, no patch flow, no upstream tracking. Sibling checkouts (`C:\code\Calyx`, `C:\code\Calyx-Dev`, ...) are never workspaces — read-only quarries at most.
